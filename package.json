{
  "name": "radialdeck",
  "version": "1.0.0",
  "description": "RadialDeck — a radial virtual keyboard overlay for creators. Float shortcut layouts over any app and fire them by touch or pen.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dist": "node build/pack.js",
    "installer": "node build/pack.js && node build/make-installer.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@electron/packager": "^20.0.1",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "png-to-ico": "^3.0.1"
  },
  "_note": "Installer is built by build/make-installer.js (a self-elevating C# bootstrapper that embeds dist/win-unpacked). electron-builder is NOT used: its winCodeSign step needs symlink privilege and the project lives on a cloud drive elevated processes can't see."
}
